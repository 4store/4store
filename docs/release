To make a release:

Make sure your git tree is in sync.
$ git tag -a 'vx.y.z' -m "Version x.y.z"
$ git push --tags
$ make dist

Copy tarball to http-a

make macdmg

Copy dmg file to http-a

Mail 4store-support@googlegroups.com with release annoucememnt.
